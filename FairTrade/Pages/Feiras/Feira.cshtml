@page
@model FairTrade.Pages.Feiras.FeiraModel
@{
}

@if (Model.erro.Length > 0)
{
    <div class='alert alert-warning alert-dismissible fade show' role='alert'>
        <strong>@Model.erro</strong>
        <button type='button' class='btn-close' data-bs-dismiss='alert' aria-label='Close'></button>
    </div>
}

<br />
@*<h2>Lista de Vendedores da feira</h2>
<table class="table">
    <thead>
        <tr>
            <th>Username</th>
            <th>Nome</th>
            <th>Rating</th>
            <th>Opção</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.listVendedores)
        {
            <tr>
                <td>@item.username</td>
                <td>@item.nome</td>
                <td>@item.rating</td>
                <td>
                    <a class="btn btn-primary btn-sm" href="/Vendedores/Vendedor/@item.id"> Aceder</a>
                </td>


            </tr>
        }
    </tbody>
</table>*@
<section class="products">
@foreach (var item in Model.listVendedores){
<div class="card">
    <div class="Title">
        @item.username
    </div>
    <div class="image">
        <img src="@item.foto" />
    </div>
</div>
}
</section>